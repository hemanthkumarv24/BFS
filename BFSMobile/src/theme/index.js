import colors from './colors';
import typography, {fontSizes, fontWeights, lineHeights} from './typography';
import spacing, {borderRadius} from './spacing';

export {colors, typography, fontSizes, fontWeights, lineHeights, spacing, borderRadius};

export default {
  colors,
  typography,
  fontSizes,
  fontWeights,
  lineHeights,
  spacing,
  borderRadius,
};
